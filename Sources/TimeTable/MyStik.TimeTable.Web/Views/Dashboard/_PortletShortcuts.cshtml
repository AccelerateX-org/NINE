@model MyStik.TimeTable.Web.Models.DashboardViewModel

<div class="portlet light bordered">
        <div class="portlet-title">
            <div class="caption">
                <span class="caption-subject bold">Schnellzugriffe</span>
            </div>
            <div class="actions">
            </div>
        </div>
    <div class="portlet-body">
        <div class="list-group">
            <a href="@Url.Action("Index", "Reservation")" class="list-group-item">
                <i class="fa fa-ticket"></i> Raumreservierungen
            </a>
            <a href="@Url.Action("Dates", "Organiser")" class="list-group-item">
                <i class="fa fa-envelope"></i> Newsletter & Mailing
            </a>
            <a href="@Url.Action("Dates", "Organiser")" class="list-group-item">
                <i class="fa fa-book"></i> Abschlussarbeiten
            </a>
            <a href="@Url.Action("Rooms", "Organiser")" class="list-group-item">
                <i class="fa fa-random"></i> Platzverlosungen
            </a>
            <a href="@Url.Action("Curricula", "Organiser")" class="list-group-item">
                <i class="fa fa-certificate"></i> Studiengänge
            </a>
        </div>

    </div>
</div>

