@model MyStik.TimeTable.Data.Thesis
    @{
        ViewBag.Title = "Löschen";
    }

    <div class="container-fluid">
        <div class="row" style="margin-top: 20px">
            <div class="col-md-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h4>Arbeit löschen</h4>
                    </div>
                    <div class="panel-body">
                        <p>fehlerhafter Eintrag. Wird ohne weitere Kommunikation gelöscht</p>
                        
                        <a href="@Url.Action("DeleteConfirmed", new {id=Model.Id})" class="btn btn-danger">Arbeit löschen</a>
                        <a href="@Url.Action("Details", new {id=Model.Id})" class="btn btn-default">Abbrechen</a>
                    </div>
                </div>
            </div>
        </div>
    </div>


