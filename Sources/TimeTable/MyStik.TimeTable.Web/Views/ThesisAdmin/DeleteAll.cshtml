    @{
        ViewBag.Title = "Löschen";
    }

    <div class="container-fluid">
        <div class="row" style="margin-top: 20px">
            <div class="col-md-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h4>Arbeiten alle löschen</h4>
                    </div>
                    <div class="panel-body">
                        <p>fehlerhafter Eintrag. Wird ohne weitere Kommunikation gelöscht</p>
                        
                        <a href="@Url.Action("DeleteAllConfirmed", new {orgId=ViewBag.OrgId})" class="btn btn-danger">Arbeiten alle löschen</a>
                        <a href="@Url.Action("All", new {orgId=ViewBag.OrgId})" class="btn btn-default">Abbrechen</a>
                    </div>
                </div>
            </div>
        </div>
    </div>


