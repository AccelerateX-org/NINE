@model ICollection<MyStik.TimeTable.Data.Semester>
@{
    ViewBag.Title = "Semester";
}

<div class="container-fluid">
    <div class="row" style="margin-top: 20px">
        <div class="col-md-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h4>
                        <i class="fa fa-user-md"></i> Semester mit Sprechstundenterminen
                    </h4>
                </div>
                <div class="list-group">
                    @foreach (var semester in Model)
                    {
                        <a href="@Url.Action("Subscriptions", "OfficeHour", new {id = semester.Id})" class="list-group-item">
                            <h5 class="list-group-item-heading">@semester.Name</h5>
                        </a>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
