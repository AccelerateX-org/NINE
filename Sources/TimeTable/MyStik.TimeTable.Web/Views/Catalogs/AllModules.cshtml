@model ICollection<MyStik.TimeTable.Data.CurriculumModule>


@{
    ViewBag.Title = "Alle Module";
}


@section styles
{
    <link href="@Url.Content("~/Assets/libs/datatables/dataTables.min.css")" rel="stylesheet" type="text/css" />
}


<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h4>Alle Module @ViewBag.Organiser.Name</h4>
            </div>
            <div class="list-group">
                <div class="list-group-item">
                    <div class="btn-group">
                        <a href="@Url.Action("Index", "Catalogs", new {id = ViewBag.Organiser.Id} )" class="btn btn-outline-primary">
                            <i class="bi-chevron-compact-left"></i> Zurück
                        </a>
                        @if (ViewBag.UserRight.IsCurriculumAdmin)
                        {
                            <a href="@Url.Action("MoveModules")" class="btn btn-outline-primary">
                                <i class="bi-list-check"></i> Zusammensetzung
                            </a>
                        }
                    </div>
                </div>
            </div>
            <table id="tabCatalogs" class="table">
                <thead>
                    <tr>
                        <th>Katalog</th>
                        <th>Tag</th>
                        <th>Bezeichnung</th>
                        <th>Modulverantwortung</th>
                        <th>Fächer</th>
                        <th>Zuordnung SG</th>
                        <th>@ViewBag.CurrentSemester.Name</th>
                        <th>@ViewBag.NextSemester.Name</th>
                    </tr>
                </thead>
                <tbody>
                        @foreach (var module in Model)
                        {
                            var hasCurrentDesc = module.Descriptions.Any(x => x.Semester.Id == ViewBag.CurrentSemester.Id) &&
                                                 module.Accreditations.Any(x => x.ExaminationDescriptions.Any(y => y.Semester.Id == ViewBag.CurrentSemester.Id));
                            var hasNextDesc = module.Descriptions.Any(x => x.Semester.Id == ViewBag.NextSemester.Id) &&
                                              module.Accreditations.Any(x => x.ExaminationDescriptions.Any(y => y.Semester.Id == ViewBag.NextSemester.Id));

                            var moduleName = string.IsNullOrEmpty(module.Name) ? "(keine Bezeichnung)" : module.Name;

                            <tr>
                                <td>@module.Catalog.Tag</td>
                                <td>@module.Tag</td>
                                <td>
                                    <a href="@Url.Action("Details", "ModuleDescription", new {id = module.Id})">
                                        @moduleName
                                    </a>
                                </td>
                                @if (module.ModuleResponsibilities.Any())
                                {
                                    <td>
                                        @foreach (var mv in module.ModuleResponsibilities)
                                        {
                                            <div>@mv.Member.FullName</div>
                                        }
                                    </td>
                                }
                                else
                                {
                                    <td>vakant</td>
                                }
                                <td>
                                    @foreach (var subject in module.ModuleSubjects)
                                    {
                                        <div>@subject.Tag (@subject.TeachingFormat.Tag)</div>
                                    }
                                </td>
                                <td>
                                    @foreach (var accreditation in module.Accreditations)
                                    {
                                        <div>@accreditation.Slot.Tag</div>
                                    }
                                </td>
                                @if (hasCurrentDesc)
                                {
                                    <td><i class="bi-check text-success"></i><span hidden="">1</span></td>
                                }
                                else
                                {
                                    <td><i class="bi-x text-danger"></i><span hidden="">0</span></td>
                                }
                                @if (hasNextDesc)
                                {
                                    <td><i class="bi-check text-success"></i><span hidden="">1</span></td>
                                }
                                else
                                {
                                    <td><i class="bi-x text-danger"></i><span hidden="">0</span></td>
                                }
                            </tr>
                        }
                </tbody>
            </table>
        </div>
    </div>
</div>


@section scripts
{
    <script src="@Url.Content("~/Assets/libs/datatables/dataTables.min.js")" type="text/javascript"></script>

    <script>
        $(document).ready(function () {
            var t = $('#tabCatalogs').DataTable({
                "paging": false,
                "order": [[0, 'asc']]
            });
        });
    </script>
}