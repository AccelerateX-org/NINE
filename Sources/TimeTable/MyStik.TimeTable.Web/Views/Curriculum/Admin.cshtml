@model MyStik.TimeTable.Data.Curriculum
@{
    ViewBag.Title = "Studiengang";
}


<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <div class="row">
                    <div class="col-md-10">

                        <h4>Verwaltung Studiengang @Model.Name (@Model.ShortName)</h4>
                        <h5 class="card-subtitle">
                            <a href="@Url.Action("Index", "Curricula")">HM</a>
                            |
                            <a href="@Url.Action("Index", "Curricula", new { id = Model.Organiser.Id })">
                                @Model.Organiser.Name (@Model.Organiser.ShortName)
                            </a>
                        </h5>
                    </div>
                    <div class="col-md-2">
                        @if (ViewBag.UserRight.IsCurriculumAdmin)
                        {
                            <div class="d-md-flex justify-content-md-end">
                                <a href="@Url.Action("Details", new {id = Model.Id})" class="btn btn-outline-primary">
                                    <i class="bi-window"></i> Ansicht
                                </a>
                            </div>
                        }
                    </div>
                </div>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-3">
                        <div class="list-group">
                            <div class="list-group-item list-group-item-dark">
                                <h5>Allgemeines</h5>
                            </div>
                            <a href="@Url.Action("Edit", "Curriculum", new {id = Model.Id})" class="list-group-item list-group-item-action" aria-current="true">
                                <div class="d-flex w-100 justify-content-between">
                                    <h5 class="mb-1">Einstellungen</h5>
                                </div>
                                <p class="mb-1">Bezeichnungen, Dauer Abschlussarbeit, Wird angeboten ja/nein</p>
                            </a>
                            <a href="@Url.Action("Labels", "Curriculum", new {id = Model.Id})" class="list-group-item list-group-item-action" aria-current="true">
                                <div class="d-flex w-100 justify-content-between">
                                    <h5 class="mb-1">Etiketten</h5>
                                </div>
                                <p class="mb-1">Aufkleber für Studiengruppen A, B, etc., Studienrichtungen, Sprache, Studienmodell usw.</p>
                            </a>
                            <a href="@Url.Action("Students", new {id = Model.Id})" class="list-group-item list-group-item-action" aria-current="true">
                                <div class="d-flex w-100 justify-content-between">
                                    <h5 class="mb-1">Studierende</h5>
                                </div>
                                <p class="mb-1">Liste aller Studierender (inkl. Alumni)</p>
                            </a>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="list-group">
                            <div class="list-group-item list-group-item-dark">
                                <h5>Aufbau (alt)</h5>
                            </div>
                            <a href="@Url.Action("GroupList", "Curricula", new { id = Model.Id })" class="list-group-item list-group-item-action" aria-current="true">
                                <div class="d-flex w-100 justify-content-between">
                                    <h5 class="mb-1">Studien- und Kapazitätsgruppen</h5>
                                </div>
                                <p class="mb-1">Erstellung, Löschen. Sind die Grundlagen für die Semestergruppen. Die Daten werden beim Import der Datei "curriculum.vsc" benötigt.</p>
                            </a>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="list-group">
                            <div class="list-group-item list-group-item-dark">
                                <h5>Aufbau (neu)</h5>
                            </div>
                            <a href="@Url.Action("Import", "Curriculum", new { id = Model.Id })" class="list-group-item list-group-item-action" aria-current="true">
                                <div class="d-flex w-100 justify-content-between">
                                    <h5 class="mb-1">Import Modulplan nach Semestern</h5>
                                </div>
                                <p class="mb-1">Version 1 - Die Slots sind nach Semestern organisiert.</p>
                            </a>
                            <a href="@Url.Action("ImportAreas", "Curriculum", new { id = Model.Id })" class="list-group-item list-group-item-action" aria-current="true">
                                <div class="d-flex w-100 justify-content-between">
                                    <h5 class="mb-1">Import Modulplan nach Themengebieten</h5>
                                </div>
                                <p class="mb-1">Version 2 - Die Slots sind nach Themengebieten organisiert.</p>
                            </a>
                            <a href="@Url.Action("Accreditation", "Curriculum", new { id = Model.Id })" class="list-group-item list-group-item-action" aria-current="true">
                                <div class="d-flex w-100 justify-content-between">
                                    <h5 class="mb-1">Import Modulakkreditierungen</h5>
                                </div>
                                <p class="mb-1">Verknüpfung zwischen den Modulen der Modulkataloge und dem Modulplan.</p>
                            </a>
                            <a href="@Url.Action("Opportunities", "Curriculum", new { id = Model.Id })" class="list-group-item list-group-item-action" aria-current="true">
                                <div class="d-flex w-100 justify-content-between">
                                    <h5 class="mb-1">Import Lehrangebot eines Semesters</h5>
                                </div>
                                <p class="mb-1">Verknüpfung zwischen den importierten Lehrveranstaltungen und den Fächern eines Moduls.</p>
                            </a>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="list-group">
                            <div class="list-group-item list-group-item-dark">
                                <h5>Löschen</h5>
                            </div>
                            <a href="@Url.Action("DeleteModulePlan", "Curriculum", new { id = Model.Id })" class="list-group-item list-group-item-primary list-group-item-action" aria-current="true">
                                <div class="d-flex w-100 justify-content-between">
                                    <h5 class="mb-1">Modulplan löschen</h5>
                                </div>
                                <p class="mb-1">Löscht alle Akkreditierungen. Keine Auswirkungen auf Modulkatalog und Lehrveranstaltungen.</p>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

