@model MyStik.TimeTable.Web.Models.ForgotPasswordViewModel
@{
    ViewBag.Title = Resources.ForgotPasswordHeading;
}

@*
    <div class="row justify-content-md-center" style="margin-bottom: 20px">
        <div class="col-md-6">
            <div class="alert alert-danger">
                <h1>Störung des Mailversands</h1>
                <p>Aktuell ist kein Versand von Mails möglich. Damit stehen Funktionen wie Registrieren, Passwort zurücksetzen nicht zur Verfügung. Ebeneso können keine Mails an Teilnehmer:innen von Lehrveranstaltungen, Newsletter, etc. versendet werden.</p>
                <p>Es wird mit Hochdruck an einer Lösung gearbeitet.</p>
                <p>Prof. Dr.-Ing. Olav Hinz</p>
            </div>
        </div>
    </div>
*@

<div class="row justify-content-center">
    <div class="col-md-4">
        <div class="card">
            <div class="card-header">
                <h4>@Resources.ForgotPasswordHeading</h4>
            </div>
            <div class="card-body">

                @using (Html.BeginForm("ForgotPassword", "Account", FormMethod.Post, new { @class = "form", role = "form" }))
                {
                    @Html.AntiForgeryToken()
                    <p>@Resources.ForgotPasswordMessage</p>
                    @Html.ValidationSummary("", new { @class = "text-danger" })

                    <div class="input-group mb-3">
                        <span class="input-group-text" id="basic-addon1"><i class="fas fa-envelope fa-fw"></i></span>
                        @Html.TextBoxFor(m => m.Email, new { @class = "form-control", placeholder = Resources.EmailPlaceholder })
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-outline-primary btn-block">
                            <i class="fas fa-paper-plane"></i>
                            @Resources.ForgotPasswordSubmitButton
                        </button>
                    </div>
                }

            </div>
        </div>
    </div>
</div>
