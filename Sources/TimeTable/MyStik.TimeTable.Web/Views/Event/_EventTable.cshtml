@model ICollection<MyStik.TimeTable.Data.Event>
<table class="table table-sm">
    <thead>
        <tr>
            <th>Name</th>
            <th># Termine</th>
            <th>Erster Termin</th>
        </tr>
    </thead>
    <tbody id="CourseList">
        @foreach (var course in Model)
        {
            @Html.Partial("_CreateEventSuccess", course)
        }
    </tbody>
</table>
