@model ICollection<MyStik.TimeTable.Data.ScriptDocument>
@{
    ViewBag.Title = "Meine Skripte";
}


@section styles
{
    <link href="@Url.Content("~/Assets/libs/datatables.net-bs4/css/dataTables.bootstrap4.min.css")" rel="stylesheet" type="text/css" />
}


@section scripts
{
    <script src="@Url.Content("~/Assets/libs/datatables.net/js/jquery.dataTables.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Assets/libs/datatables.net-bs4/js/dataTables.bootstrap4.min.js")" type="text/javascript"></script>

    <script>
        $(document).ready(function () {
            $('#tabCourses').DataTable({
                "paging": false
            });
        });
    </script>
}


<div class="row">
    <div class="col-md-12">
        <div class="alert alert-info" role="alert">
            <h5>Bereitstellung von Skripten für Skriptendruck</h5>
            <p>Ein "Skriptenteam" organisiert für eine Fakultät den Druck und die Verteilung von Skripten in Papierform. Auf dieser Seite kann ich als Lehrende:r meine Skripte hochladen und Lehrveranstaltungen zuordnen. Folgende Hinweise:</p>
            <ul>
                <li>Skripte können nur von Teilnehmer:innen einer Lehrveranstaltung bestellt werden.</li>
                <li>Jedes Skript kann von einer Person nur einmal bestellt werden, auch wenn in mehreren Lehrveranstaltungen eingetragen und Skript dort publiziert ist (z.B. bei Parallelveranstaltungen).</li>
                <li>Der Download der Dokumente erfolgt über das "Skriptenteam". Dieses benötigt einen NINE-Account. Studierende haben keine Möglichkeiten, die Dokumente direkt herunterzuladen</li>
                <li>Die Möglichkeiten hier sind aktuell rudimentär und decken nur den "Normalfall" ab: Eine Lehrveranstaltung - ein Skript</li>
            </ul>
        </div>

        <div class="card">
            <div class="card-header">
                Meine Skripte
            </div>
            <ul class="nav bg-light text-dark">
                <li class="nav-item active">
                    <a class="nav-link text-center" href="@Url.Action("Create")"><i class="fas fa-plus fa-fw"></i><br />Neues Skript</a>
                </li>
            </ul>
            <table id="tabCourses" class="table table-striped table-bordered table-condensed">
                <thead>
                    <tr>
                        <th>Titel</th>
                        <th>Version</th>
                        <th>Publiziert seit</th>
                        <th>Publiziert für</th>
                        <th>Anzahl Bestellungen</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var doc in Model)
                {
                    <tr>
                        <td>@doc.Title</td>
                        <td>@doc.Version</td>
                        <td>@doc.Created.ToShortDateString()</td>
                        <td>
                            @foreach (var pub in doc.Publishings)
                            {
                                <div>@pub.Course.Name (@pub.Course.ScriptPublishings.Count)</div>
                            }
                        </td>
                        <td>@doc.Orders.Count</td>
                        <td>
                            <a href="@Url.Action("GetDocument", "Storage", new {id = doc.Storage.Id})" class="btn btn-outline-primary">Download</a>
                            <a href="@Url.Action("Edit", new {id = doc.Id})" class="btn btn-outline-primary">Titel / Version ändern</a>
                            @if (doc.Orders.Count == 0)
                            {
                                <a href="@Url.Action("Delete", "ScriptPublishing", new {id = doc.Id})" class="btn btn-outline-danger">Löschen</a>
                            }
                            else
                            {
                                <div class="btn btn-outline-danger disabled">Löschen</div>
                            }
                        </td>
                    </tr>
                }

                </tbody>
            </table>
        </div>
    </div>
</div>

