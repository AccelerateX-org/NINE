@model MyStik.TimeTable.Data.ScriptDocument
@{
    ViewBag.Title = "Skript ändern";
}


<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                Angaben zu Skript ändern
            </div>
            <div class="card-body">
                @using (Html.BeginForm(null, null, FormMethod.Post, new { @class = "form-horizontal"}))
                {
                    @Html.HiddenFor(m => m.Id)
                    <div class="form-group row">
                        <label for="Title" class="col-sm-6 col-form-label">Titel (eindeutige Bezeichnung, z.B. "Technische Mechanik", "Grundlagen der Informatik - Aufgabensammlung")</label>
                        <div class="col-sm-6">
                            @Html.TextBoxFor(m => m.Title, new {@class = "form-control"})
                        </div>
                    </div>
                    <div class="form-group row">
                        <label for="Version" class="col-sm-6 col-form-label">Version (beliebige kennzeichnung, z.B. 3.7, WiSe 2020</label>
                        <div class="col-sm-6">
                            @Html.TextBoxFor(m => m.Version, new {@class = "form-control"})
                        </div>
                    </div>

                    <div class="form-group row">
                        <div class="col-sm-6">
                            <button type="submit" class="btn btn-outline-primary"><i class="fas fa-save"></i> Speichern</button>
                            <a href="@Url.Action("Index")" class="btn btn-outline-secondary"><i class="fas fa-times"> </i>Abbrechen</a>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

