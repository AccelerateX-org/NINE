@model MyStik.TimeTable.Web.Models.ScriptCreatetModel
@{
    ViewBag.Title = "Neues Skript";
}


<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                Neues Skript anlegen
            </div>
            <div class="card-body">
                <div class="alert alert-warning" role="alert">
                    <h5>Hinweis:</h5>
                    <p>Die Verknüpfung des Skripts zu Modulen und Lehrveranstaltungen kann später noch beliebig ergänzt und verändert werden.</p>
                </div>
                @using (Html.BeginForm(null, null, FormMethod.Post, new {enctype = "multipart/form-data"}))
                {
                    @Html.HiddenFor(m => m.SemesterId)
                    <div class="row mb-3">
                        <label for="Title" class="col-sm-6 col-form-label">Titel (eindeutige Bezeichnung, z.B. "Technische Mechanik", "Grundlagen der Informatik - Aufgabensammlung")</label>
                        <div class="col-sm-6">
                            <input class="form-control" type="text" id="Title" name="Title">
                        </div>
                    </div>
                    <div class="row">
                        <label for="Version" class="col-sm-6 col-form-label">Version (beliebige Kennzeichnung, z.B. 3.7, WiSe 2020</label>
                        <div class="col-sm-6">
                            @Html.TextBoxFor(m => m.Version, new {@class = "form-control"})
                        </div>
                    </div>
                    <div class="row">
                        <label for="ScriptDoc" class="col-sm-6 control-label">Datei (pdf-Format)</label>
                        <div class="col-sm-6">
                            <input class="form-control" type="file" id="ScriptDoc" name="ScriptDoc" accept="application/pdf">
                        </div>
                    </div>
                    <div class="row">
                        <label for="Semester" class="col-sm-6 col-form-label">Semester</label>
                        <div class="col-sm-6">
                            <input class="form-control" type="text" value="@Model.Semester.Name" aria-label="readonly input example" readonly name="Semester">
                        </div>
                    </div>
                    <div class="row">
                        <label for="Module" class="col-sm-6 col-form-label">Modul</label>
                        <div class="col-sm6">
                            @Html.DropDownListFor(m => m.Module, (IEnumerable<SelectListItem>) ViewBag.Modules, new {@class = "form-control"})
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-sm-12">
                            @Html.CheckBoxFor(m => m.AllCourses)
                            <label for="IsAvailable" class="col-sm-10 col-form-label">Für alle Lehrveranstaltung gleichen Namens</label>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-sm-6">
                            <button type="submit" class="btn btn-outline-primary"><i class="bi-save"></i> Speichern</button>
                            <a href="@Url.Action("Index")" class="btn btn-outline-secondary"><i class="bi-x-circle"> </i>Abbrechen</a>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

